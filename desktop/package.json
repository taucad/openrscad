{
  "name": "openrscad-desktop",
  "version": "0.11.0-beta.3",
  "private": true,
  "description": "OpenRSCAD desktop app (Tauri v2) — native OpenSCAD-compatible renderer.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --config src-tauri/tauri.conf.json",
    "build": "tauri build --config src-tauri/tauri.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
